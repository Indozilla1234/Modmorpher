ModMorpher — Porting Notes
============================================================

These items could not be automatically converted and require
manual attention before the addon will be fully functional.

Network Packets
---------------
  UNKNOWN packet 'MudMessage' → scriptEventReceive id='earthmobsmod:mudmessage'.  Java sender must call: world.events.server.execute(() -> MinecraftServer#execute('/scriptevent earthmobsmod:mudmessage {...}')).
  UNKNOWN packet 'MossMessage' → scriptEventReceive id='earthmobsmod:mossmessage'.  Java sender must call: world.events.server.execute(() -> MinecraftServer#execute('/scriptevent earthmobsmod:mossmessage {...}')).

Client-Side Rendering
---------------------
  PigModelMixin (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BlockstateGenerator (uses client-only import package net.minecraftforge.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  ItemModelGenerator (uses client-only import package net.minecraftforge.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MudFluidType (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  ModModelLayers (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  ClientEvents (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  ClientRegistrar (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BoneSpiderRender (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  VilerWitchRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  FancyChickenRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  WoolyCowRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  HyperRabbitRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  ZombifiedPigRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  WitherSkeletonWolfRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MelonGolemRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  HornedSheepRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  CluckShroomRender (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  StrayBoneSpiderRender (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BabyGhastRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MoobloomRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  FurnaceGolemRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  JollyLlamaRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  LobberZombieRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MagmaCowRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MoolipRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  TeaCupPigRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  LobberDrownedRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  JumboRabbitRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  DuckRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BoulderingDrownedRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  SkeletonWolfRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BoulderingZombieRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  TropicalSlimeRenderer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  UmbraCowRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  RevampedCowRenderer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  SpinAttackEffectLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MuddyPigMudLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MuddyPigFlowerLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  FurnaceGolemCrackinessLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MelonGolemHeadLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  SkeletonWolfEyesLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BabyGhastCoreLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  StrayBoneSpiderEyesLayer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  HornedSheepFurLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MossSheepLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BoneSpiderEyesLayer (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  CowPlantLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  ZombieOuterLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  VilerWitchItemLayer (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BabyGhastAnimation (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  JollyLlamaAnimation (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  FancyChickenAnimations (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  JollyLlamaModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  TeaCupPigModel (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  HornedSheepModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BoulderingDrownedModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  LobberZombieModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MuddyPigModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  FurnaceGolemModel (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MagmaCowModel (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  HornedSheepFurModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  VilerWitchModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BabyGhastModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  WoolyCowModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  HyperRabbitModel (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BoneSpiderModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  FancyChickenModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  BoulderingZombieModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  JumboRabbitModel (uses client-only import package com.mojang.blaze3d): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  CluckShroomModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  LobberDrownedModel (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MossMessage (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
  MudMessage (uses client-only import package net.minecraft.client): client-side rendering has no Bedrock equivalent. Textures/models are handled by the RP; custom shaders and render layers cannot be ported.
CRITICAL FAILURE: No Bedrock equivalent for Java method: m_128379_
CRITICAL FAILURE: No Bedrock equivalent for Java method: m_128379_
CRITICAL FAILURE: No Bedrock equivalent for Java method: m_128344_
CRITICAL FAILURE: No Bedrock equivalent for Java method: m_128379_
[loader] detected project loader: forge
